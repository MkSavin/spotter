# ── server · layer 2 ──────────────────────────────────────────────────
# Service-specific overrides, layered on top of the shared `.env`
# (`cp .env.server.example .env.server`). Inherits REDIS_URL / S3_* / TZ.
REDIS_GROUP_ID=spotter-server
DATABASE_PATH=./data/server.sqlite
SOURCE_ID=frigate
