{
  "name": "@spotter/server",
  "version": "1.0.0",
  "description": "Headless domain service: event persistence, media orchestration, auth, command RPC",
  "main": "index.js",
  "scripts": {
    "start": "bun --env-file=../../.env.server src/index.ts",
    "start:watch": "bun --watch --env-file=../../.env.server src/index.ts",
    "build": "bun build --entrypoints src/index.ts src/cli.ts --outdir ./dist --target bun --env disable",
    "sign:token": "bun --env-file=../../.env.server src/cli.ts sign",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "if [ -n \"$(find src -name '*.test.ts' 2>/dev/null)\" ]; then bun test; fi",
    "test:coverage": "if [ -n \"$(find src -name '*.test.ts' 2>/dev/null)\" ]; then bun test --coverage; fi"
  },
  "private": "true",
  "sideEffects": "false",
  "keywords": [],
  "author": "me@mksav.in",
  "license": "MIT",
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  },
  "dependencies": {
    "@spotter/transport": "*",
    "commander": "^13.1.0",
    "drizzle-orm": "^0.45.2",
    "stenograph": "*"
  }
}
