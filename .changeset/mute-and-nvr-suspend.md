---
'@spotter/transport': minor
'@spotter/sink': minor
'@spotter/telegram': minor
'@spotter/frigate': minor
'@spotter/forwarder': patch
---

feat: add silence controls. `/mute` and `/unmute` silence a single chat, and `/nvr_suspend` (ADMIN) suspends the NVR's own notifications through the new `NotificationSuspender` port
